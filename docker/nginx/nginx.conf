events {}

http {
    log_format combined_log '$remote_addr - - [$time_local] '
                            '"$request" $status $body_bytes_sent '
                            '"$http_referer" "$http_user_agent"';

    server {
        listen 80;

        location /dvwa/ {
            access_log /var/log/nginx/dvwa/access.log combined_log;
            proxy_pass http://dvwa:80/;
        }

        location /juice/ {
            access_log /var/log/nginx/juice_shop/access.log combined_log;
            proxy_pass http://juice_shop:3000/;
        }

        location /webgoat/ {
            access_log /var/log/nginx/webgoat/access.log combined_log;
            proxy_pass http://webgoat:8080/;
        }
    }
}
